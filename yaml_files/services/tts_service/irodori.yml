# Irodori TTS Service Configuration
# Irodori API server should be running separately:
#   python irodori_api_server.py --port 8000

tts_config:
  type: "irodori"
  configs:
    base_url: "http://192.168.0.41:8091"
    ref_audio_dir: "/data2/yw0nam/data/reference_voices"  # Path to voices dir ({name}/audio.wav); null for no-reference mode
    seconds: 30.0
    num_steps: 40
    cfg_scale_text: 3.0
    cfg_scale_speaker: 5.0
    seed: null
